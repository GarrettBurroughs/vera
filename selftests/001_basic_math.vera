// run-pass
// expect-exit-code: 0

pub func main(): i32 {
    const a: i32 = 1;
    const b: i32 = 1;
    
    if a + b == 2 {
        return 0; // Success exit code
    } else {
        return 1; // Failure exit code
    }
}
