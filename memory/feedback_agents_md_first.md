---
name: feedback-agents-md-first
description: Must read agents.md before taking any action in this repository
metadata:
  type: feedback
---

Always read `agents.md` in the repository root before taking any action.

**Why:** The repository has strict workspace rules documented there (no bash file editing, TDD required, documentation as source of truth, context window efficiency guidelines). Missing this causes rule violations.

**How to apply:** The very first action in any session working on this repo must be `Read agents.md`. Then read `development.md` and `roadmap.md` as directed.
